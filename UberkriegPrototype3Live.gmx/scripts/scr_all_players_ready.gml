var allready = false ;
var i = 1;
var players_ready = 0 ;

for ( i = 1 ; i < 5 ; i++ ){
    if (global.P_ready[i] = true) players_ready++
    }

if (players_ready = global.number_of_players and global.Host_ready) allready = true;

return allready ;
